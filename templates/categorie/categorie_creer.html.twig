{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="container">
        <h1>{{ title }}</h1>
        {{ form_start(form) }}
            {{ form_row(form.label) }}
            <button type="submit" class="btn btn-primary">Creer la catégorie</button>
        {{ form_end(form) }}
    </main>
{% endblock %}
