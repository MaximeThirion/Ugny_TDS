{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="container">
        <h1>{{ title }}</h1>
        {{ form_start(form) }}
            {{ form_row(form.titre) }}
            {{ form_row(form.sous_titre) }}
            {{ form_row(form.contenu) }}
            {{ form_row(form.file) }}
            <button type="submit" class="btn btn-primary">Creer l'activité</button>
        {{ form_end(form) }}
    </main>
{% endblock %}